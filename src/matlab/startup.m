%STARTUP adds required paths upon initiation.
%   The script uses the function 'paths' to add required paths.
%
%   Author: Andreas Brink-Kjaer.
%   Date: 24-Oct-2022
%

%% Set paths
dirIndex = paths;
config = Config(dirIndex);
